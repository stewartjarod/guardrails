{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "baseline scan \"$CLAUDE_FILE_PATH\" --config baseline.toml --format json",
        "description": "Run Baseline after file edits to catch violations immediately"
      }
    ]
  }
}
